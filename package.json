{
  "name": "visual-demo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^12.9.0",
    "three": "^0.183.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "jsdom": "^28.1.0",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
